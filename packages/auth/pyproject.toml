[project]
name = "auth"
version = "0.1.0"
description = "认证与会话管理模块"
requires-python = ">=3.12"
dependencies = [
    "core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/auth"]
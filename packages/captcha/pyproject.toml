[project]
name = "captcha"
version = "0.1.0"
description = "验证码处理模块"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/captcha"]

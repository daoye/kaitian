[project]
name = "stealth"
version = "0.1.0"
description = "反检测/反反爬虫模块"
requires-python = ">=3.12"
dependencies = [
    "core",
    "playwright-stealth>=2.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stealth"]
